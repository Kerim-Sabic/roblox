{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum APIs required by the bundled NectarPilot dashboard",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "log:default",
    "window-state:default"
  ]
}
